<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.codymorrow.cslearner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LanguageChoices.HomeScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LanguageChoices.ContextLanguage" />
        <activity android:name=".Comparison.LanguageComparisonHome" />
        <activity android:name=".Comparison.ConceptComparison" />
        <activity android:name=".Comparison.VariableComparison" />
        <activity android:name=".Comparison.ConditionalComparison" />
        <activity android:name=".Comparison.LoopComparison" />
        <activity android:name=".Comparison.MethodComparison"></activity>
    </application>

</manifest>